# Decision records

ADRs for choices whose *reasoning* is worth keeping even if a future project picks differently. Not every choice gets one — only ones with a real trade-off behind them. Numbered sequentially, never renumbered or deleted; a reversed decision gets a new ADR that supersedes the old one (mark the old one's Status as Superseded, don't delete it).
